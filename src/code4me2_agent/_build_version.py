"""Runtime version used for source checkouts.

Release and local ZIP builds temporarily replace this value with their immutable
artifact version via ``packaging/stamp_runtime_version.py``.
"""

RUNTIME_VERSION = "0.0.0-dev"
